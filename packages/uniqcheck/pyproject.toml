[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "uniqcheck"
version = "0.1.0"
description = "CI-friendly data checks built on top of uniqdiff."
readme = "README.md"
requires-python = ">=3.9"
license = "Apache-2.0"
authors = [
  { name = "UniqTools maintainers" }
]
dependencies = [
  "uniqdiff>=1.0,<2.0",
]

[project.scripts]
uniqcheck = "uniqcheck.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
uniqcheck = ["py.typed"]
